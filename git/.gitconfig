[user]
	name = Chiel Heldoorn
	email = chiel_heldoorn@hotmail.com
    username = ChielWH

[web]
	browser = safari

[alias]
    st = "status -s -b"
    c = "commit"
    cm = "commit -m"
    cam = "commit -am"
    camend = "commit --amend"

    # --all
    # --verbose
    br = "branch -a -v"

    co = "checkout"
    cob = "checkout -b"

    # Short hash, relative date and message.
    logd = "log --pretty='%C(yellow)%h %C(cyan)%ar %Creset%s'"
